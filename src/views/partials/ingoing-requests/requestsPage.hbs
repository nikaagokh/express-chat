<div class="content-container">
    {{#notEmpty requests}}
    <h2>მეგობრობის თხოვნები</h2>
    <div class="list" id="ingoingRequestsList">
        {{#each requests as |request i|}}
        <div class="list-item list-divider" data-user-id="{{request.user_id}}" data-user-name="{{request.user_name}}">
            <i class="material-symbols-outlined list-item-avatar">account_circle</i>
            <span class="list-item-content">
                <h4 class="list-item-title">{{request.full_name}}</h4>
            </span>
            <button class="button button-icon" data-tooltip="მეტის ნახვა">
                <i class="material-symbols-outlined">more_horiz</i>
            </button>
        </div>
        {{/each}}
    </div>
    {{else}}
    <div class="empty-list-wrapper">
        <img src="/public/images/empty.webp" width="300" height="300">
        <div class="empty-search-suggestion">
            <h3>მეგობრების თხოვნები ცარიელია ! </h3>
        </div>
    </div>
    {{/notEmpty}}
</div>